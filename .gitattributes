# 將所有檔案的換行符在 Git 儲存時都統一轉換為 LF (Unix 格式)
# 當 checkout (拉取) 檔案時，會根據作業系統自動轉換
# * text=auto eol=lf
text=auto eol=lf

# 明確指定 .bat 檔案使用 Windows 換行符 (CRLF)。保留 Windows 指令檔為 CRLF
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf